$(document).ready(function(){
	// console.log("javascript loaded");

	// /* ---- DEFINE VARIABLES ---- */
	// var pageIsLoading = true,
	// 	itemIsActive = false;

	// var tlLoader = new TimelineMax( { paused: false }),
	// 	tlIcons = new TimelineMax( { paused: false }),


	// /* ---- SET TIMELINE ---- */
	// // tlLoader.set("svg#rups circle", { y: 16 });


	// $('body').on("click touchstart", showClickPing);
	// $('.menu .open-gedrag').on("click touchstart", openMenuItem);
	// $('.movie').on("click touchstart", openMovie);
	// $(movieCloseSVG).on("click touchstart", closeMovie);
	// $(pageCloseSVG).on("click touchstart", closePage);

});